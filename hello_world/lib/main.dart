import 'package:flutter/material.dart';
// import 'entrance_fader.dart';

void main() => runApp(CardsExample());

class CardsExample extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Cards'),
      ),
      body: Text('Hello World'),
    );
  }
}
